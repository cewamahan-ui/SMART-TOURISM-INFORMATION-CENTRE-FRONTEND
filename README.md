# SafariSmart Frontend

A modern React frontend for the Smart Tourism Information Centre experience, built with TanStack Router + Vite.

It includes:
- Public exploration flows (explore, itinerary, transport, accommodations, bookings)
- Kiosk-first landing and preferences flow
- Favorites and notifications screens
- Authentication screens (login/register/profile/settings)
- Multi-language UI support
- Backend API integration through a centralized client

## Tech Stack

- React 19
- Vite 7
- TanStack Router + TanStack Query
- Tailwind CSS 4
- Radix UI + custom UI primitives
- Lucide icons

## Project Structure

```text
src/
  components/        Reusable UI and layout pieces
  hooks/             React hooks (e.g. mobile breakpoint detection)
  language/          i18n provider and locale dictionaries
  lib/               API client, auth context, utilities
  routes/            File-based route pages
  routeTree.gen.ts   Generated by TanStack Router
  routeTree.gen.jsx  Synced JSX variant used by the app
scripts/
  route-tree-sync.mjs  Keeps routeTree.gen.jsx in sync
```

## Prerequisites

- Node.js 18+
- npm 9+

## Getting Started

1. Install dependencies:

```bash
npm install
```

2. Create your environment file:

```bash
cp .env.example .env
```

If you do not have `.env.example`, create `.env` manually with:

```env
VITE_API_BASE_URL=http://localhost:5000
```

3. Start development:

```bash
npm run dev
```

The dev script automatically:
- syncs route tree files once
- starts a route-tree watcher
- runs Vite dev server

## Available Scripts

- `npm run dev` - Sync route tree, watch route tree, and start dev server
- `npm run build` - Sync route tree and build production bundle
- `npm run build:dev` - Build using development mode
- `npm run preview` - Preview production build locally
- `npm run lint` - Run ESLint
- `npm run format` - Run Prettier format
- `npm run sync:route-tree` - One-time sync from `routeTree.gen.ts` to `routeTree.gen.jsx`
- `npm run watch:route-tree` - Watch and continuously sync route tree files

## Routing Notes

This project uses TanStack file-based routing.

Important:
- Keep `src/routeTree.gen.ts` present (TanStack generator depends on it).
- The sync script transpiles that generated TypeScript file into `src/routeTree.gen.jsx`.
- If routes change, run `npm run sync:route-tree`.

## API Integration

Main API client lives in `src/lib/api.jsx`.

- Base URL comes from `VITE_API_BASE_URL`.
- Access token is stored in local storage (`sts_access_token`).
- The client supports JSON payload helpers and normalized error handling.

The client includes modules for:
- auth
- destinations and attractions
- itineraries and bookings
- transport
- payments
- kiosk sessions and operations
- favorites, users, audit, and admin endpoints

## Internationalization (i18n)

Language support is implemented with:
- `src/language/i18n-provider.jsx`
- `src/language/locales/*`

Supported locale dictionaries include English, Swahili, French, and German.

## Main Routes

Key route files include:
- `/` (`index.jsx`)
- `/kiosk-preferences`
- `/explore`
- `/itinerary`
- `/transport`
- `/accommodations`
- `/bookings`
- `/notifications`
- `/favorites` and `/favourites` alias
- `/field-guide`
- `/settings`
- `/profile`
- `/login`
- `/register`
- `/sos`

## Troubleshooting

- Route issues after edits:
  - Run `npm run sync:route-tree`.
- Backend not reachable:
  - Check `VITE_API_BASE_URL` and ensure backend is running.
- Menu/layout weirdness during active dev reload:
  - Hard refresh browser after major layout refactors.

## License

Private/internal project unless your team specifies otherwise.



# React + Vite

This template provides a minimal setup to get React working in Vite with HMR and some ESLint rules.

Currently, two official plugins are available:

- [@vitejs/plugin-react](https://github.com/vitejs/vite-plugin-react/blob/main/packages/plugin-react) uses [Oxc](https://oxc.rs)
- [@vitejs/plugin-react-swc](https://github.com/vitejs/vite-plugin-react/blob/main/packages/plugin-react-swc) uses [SWC](https://swc.rs/)

## React Compiler

The React Compiler is enabled on this template. See [this documentation](https://react.dev/learn/react-compiler) for more information.

Note: This will impact Vite dev & build performances.

## Expanding the ESLint configuration

If you are developing a production application, we recommend using TypeScript with type-aware lint rules enabled. Check out the [TS template](https://github.com/vitejs/vite/tree/main/packages/create-vite/template-react-ts) for information on how to integrate TypeScript and [`typescript-eslint`](https://typescript-eslint.io) in your project.
